namespace HMS_API.Enums
{
    public enum ScheduleStatus
    {
        Scheduled,
        Canceled,
        Completed
    }

    public enum TransactionStatus
    {
        Pending,
        Successful,
        Failed
    }

}
